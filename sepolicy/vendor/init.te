allow init adsprpcd_file:filesystem { mount relabelfrom relabelto };
allow init vendor_firmware_file:dir mounton;
allow init mnt_vendor_file:dir mounton;
allow init vendor_firmware_file:filesystem { relabelto relabelfrom mount };
allow init persist_drm_file:filesystem { relabelto relabelfrom mount associate };
allow persist_drm_file self:filesystem { associate };
